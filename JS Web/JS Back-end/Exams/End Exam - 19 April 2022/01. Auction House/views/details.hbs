
            <!-- Details Page -->
            <section id="catalog-section">

                <h1 class="item">
                    {{auction.title}}
                    {{#if auction.isAuthor}}
                    <div class="f-right">
                        <a class="action pad-small f-left" href="#">Edit</a>
                        <a class="action pad-small f-left" href="#">Delete</a>
                    </div>
                    {{/if}}
                </h1>
                <div class="item padded">

                    <div class="layout right large">

                        <div class="col">
                            <img src="{{auction.imageUrl}}" class="img-large">
                        </div>

                        <div class="content pad-med">

                            <p>In category: <strong>{{auction.category}}</strong></p>
                            <p>{{auction.description}}</p>

                            <div class="align-center">
                                <div>
                                    Current price: $<strong>{{auction.price}}</strong>
                                </div>

                                {{#if auction.hasUser}}
                                {{#if auction.isAuthor}}
                                {{else}}
                                {{#if auction.bidder}}
                                <!-- If current user is the bidder -->
                                <div>
                                    You are currently the <strong>highest bidder</strong> for this auction
                                </div>
                                {{else}}
                                <!-- If current user can place a bid -->
                                <form class="vertical" method="POST" action="/auction/bid/{{auction._id}}">
                                    <label><span>Bid amount</span><input type="number" name="price"></label>
                                    <input class="action" type="submit" value="Place bid">
                                </form>
                                {{/if}}
                                {{/if}}
                                {{/if}}
                                <div>
                                    {{#if auction.isAuthor}}
                                    {{#if auction.bidder}}
                                    <!-- If there is a bidder -->
                                    Bid by <strong>{{auction.bidder.firstName}} {{auction.bidder.lastName}}</strong>

                                    <!-- # Bonus: If there is a bidder --><!--

                                    <a class="action pad-med cta" href="#">Close Auction</a>
                                    
                                    # End of Bonus -->
                                    {{else}}
                                    <!-- If there are no bids -->
                                    No bids
                                    {{/if}}
                                    {{/if}}
                                </div>
                            </div>

                        </div>
                    </div>

                    <footer>Listed by {{auction.bidder.firstName}} {{auction.bidder.lastName}}</footer>
                </div>

            </section>

 